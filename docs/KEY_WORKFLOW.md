# Evidence Signing Key Workflow

Visual guide to how signing keys work in the ROSIE evidence pipeline.

---

## Overview Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                     KEY GENERATION (ONE-TIME)                   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │  Generate Keys   │
                    │  openssl ecparam │
                    └────────┬─────────┘
                             │
                 ┌───────────┴───────────┐
                 │                       │
                 ▼                       ▼
        ┌────────────────┐      ┌────────────────┐
        │ Private Key    │      │ Public Key     │
        │ (SECRET)       │      │ (SHAREABLE)    │
        └────────┬───────┘      └────────┬───────┘
                 │                       │
                 │                       │
    ┌────────────┼───────────┐          │
    │            │           │          │
    ▼            ▼           ▼          ▼
┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐
│ Local  │  │GitHub  │  │Password│  │ Can    │
│.rosie- │  │Secrets │  │Manager │  │ Share  │
│ keys/  │  │(CI)    │  │(Backup)│  │Publicly│
└────────┘  └────────┘  └────────┘  └────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    EVIDENCE GENERATION (CI)                     │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │  Run Tests       │
                    │  vitest --run    │
                    └────────┬─────────┘
                             │
                             ▼
                    ┌──────────────────┐
                    │ Test Results     │
                    │ (JSON)           │
                    │ {passed:5, ...}  │
                    └────────┬─────────┘
                             │
                             ▼
                    ┌──────────────────┐
                    │ Create Payload   │
                    │ {gxp_id, spec_id,│
                    │  test_results}   │
                    └────────┬─────────┘
                             │
                             ▼
                    ┌──────────────────┐
                    │ Sign with        │
                    │ PRIVATE KEY      │◄─── From GitHub Secret
                    │ (JWS)            │
                    └────────┬─────────┘
                             │
                             ▼
                    ┌──────────────────┐
                    │ Evidence File    │
                    │ EV-SPEC-*.jws    │
                    │ (signed)         │
                    └────────┬─────────┘
                             │
                             ▼
                    ┌──────────────────┐
                    │ Commit to Git    │
                    │ .gxp/evidence/   │
                    └────────┬─────────┘
                             │
                             ▼
                    ┌──────────────────┐
                    │ Push to GitHub   │
                    └─────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                   EVIDENCE VERIFICATION (AUDITOR)               │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │ Download Evidence│
                    │ from GitHub      │
                    └────────┬─────────┘
                             │
                             ▼
                    ┌──────────────────┐
                    │ Get Public Key   │◄─── From repo or email
                    │ (no secret)      │
                    └────────┬─────────┘
                             │
                             ▼
                    ┌──────────────────┐
                    │ Verify Signature │
                    │ jose.jwtVerify() │
                    └────────┬─────────┘
                             │
                 ┌───────────┴───────────┐
                 │                       │
                 ▼                       ▼
        ┌────────────────┐      ┌────────────────┐
        │ ✅ Valid       │      │ ❌ Invalid     │
        │ Evidence       │      │ (Tampered)     │
        │ Authentic      │      │ Reject         │
        └────────────────┘      └────────────────┘
```

---

## Key Lifecycle

```
┌───────────┐
│  Day 0    │  Generate keys, add to GitHub Secrets
└─────┬─────┘
      │
      ▼
┌───────────┐
│  Day 1+   │  CI auto-signs evidence on every merge
└─────┬─────┘
      │
      ▼
┌───────────┐
│  Year 1   │  Annual rotation (best practice)
└─────┬─────┘
      │
      ▼
┌───────────┐
│  or       │  Key compromised → immediate rotation
└───────────┘
```

---

## Evidence File Anatomy

```
┌─────────────────────────────────────────────────────────┐
│ EV-SPEC-006-001-001.jws                                 │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ HEADER (base64url)                                      │
│ {                                                       │
│   "alg": "ES256",           ← Algorithm                 │
│   "typ": "JWT",             ← Type                      │
│   "kid": "rosie-2026-02-05" ← Key ID                    │
│ }                                                       │
│                                                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ PAYLOAD (base64url)                                     │
│ {                                                       │
│   "gxp_id": "EV-SPEC-006-001-001",                      │
│   "spec_id": "SPEC-006-001-001",                        │
│   "test_results": {                                     │
│     "passed": 5, "failed": 0                            │
│   },                                                    │
│   "timestamp": "2026-02-05T18:30:45Z",                  │
│   "system_state": "git:b99d0f7"                         │
│ }                                                       │
│                                                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ SIGNATURE (base64url)                                   │
│ MEUCIQDXxY1Z2...                                        │
│                                                         │
│ Created by: ECDSA(SHA256(header + payload), privateKey)│
│ Verified by: ECDSA_verify(signature, publicKey)         │
│                                                         │
└─────────────────────────────────────────────────────────┘

Actual file content (single line):
eyJhbGci...HEADER...eyJAY29u...PAYLOAD...MEUCIQDXx...SIGNATURE...
    ▲               ▲                     ▲
    │               │                     │
    └───────────────┴─────────────────────┘
         Separated by periods (.)
```

---

## Signature Verification Flow

```
┌──────────────┐
│ Evidence.jws │
└──────┬───────┘
       │
       ▼
┌──────────────────────────┐
│ Split by "."             │
│ [header, payload, sig]   │
└──────┬───────────────────┘
       │
       ├─────────────────────────────────────┐
       │                                     │
       ▼                                     ▼
┌──────────────────┐              ┌──────────────────┐
│ Decode header    │              │ Extract signature│
│ Get algorithm    │              │ (raw bytes)      │
│ (ES256)          │              │                  │
└──────┬───────────┘              └──────┬───────────┘
       │                                  │
       ▼                                  │
┌──────────────────┐                     │
│ Decode payload   │                     │
│ Get claims       │                     │
└──────┬───────────┘                     │
       │                                  │
       ▼                                  │
┌──────────────────────────────────┐     │
│ Reconstruct signing input:       │     │
│ base64url(header) + "." +        │     │
│ base64url(payload)               │     │
└──────┬───────────────────────────┘     │
       │                                  │
       ├──────────────────────────────────┤
       │                                  │
       ▼                                  ▼
┌─────────────────────────────────────────────┐
│ Verify:                                     │
│ ECDSA_verify(signingInput, signature, pubKey)│
└──────┬──────────────────────────────────────┘
       │
       ├──────────────────┐
       │                  │
       ▼                  ▼
  ┌────────┐        ┌─────────┐
  │ Valid  │        │ Invalid │
  │ ✅     │        │ ❌      │
  └────────┘        └─────────┘
```

---

## Key Storage Comparison

| Location | Security | Accessibility | Backup | Cost |
|----------|----------|---------------|--------|------|
| **Local (.rosie-keys/)** | 🟡 Medium (file permissions) | ✅ High (always available) | ❌ No (machine-specific) | Free |
| **GitHub Secrets** | ✅ High (encrypted at rest) | 🟡 Medium (CI only) | ❌ No (can't retrieve) | Free |
| **Password Manager** | ✅ Very High (end-to-end encrypted) | ✅ High (cloud sync) | ✅ Yes (automatic) | ~$5/mo |
| **Environment Variables** | ❌ Low (visible in process list) | ✅ High | ❌ No | Free |
| **Git Repo** | ❌ NEVER | ✅ Very High | ✅ Yes | ❌ NEVER |

**Recommended Strategy:**
- ✅ Primary: GitHub Secrets (for CI)
- ✅ Backup: Password Manager (for recovery)
- ✅ Development: Local (.rosie-keys/)
- ❌ Never: Git, Environment Variables

---

## Common Workflows

### **Workflow 1: Initial Setup**

```
Developer                    GitHub                  CI
   │                           │                      │
   │─ Generate keys            │                      │
   │  (local machine)          │                      │
   │                           │                      │
   │─ Add to Secrets ─────────▶│                      │
   │  (EVIDENCE_PRIVATE_KEY)   │                      │
   │  (EVIDENCE_PUBLIC_KEY)    │                      │
   │                           │                      │
   │─ git push ───────────────▶│                      │
   │                           │                      │
   │                           │─ Trigger workflow ──▶│
   │                           │                      │
   │                           │                      │─ Load secrets
   │                           │                      │─ Run tests
   │                           │                      │─ Sign evidence
   │                           │                      │─ Commit evidence
   │                           │◀─────────────────────│
   │                           │                      │
   │◀─ Evidence committed ─────│                      │
```

### **Workflow 2: Key Rotation**

```
Developer                    GitHub                  CI
   │                           │                      │
   │─ Generate NEW keys        │                      │
   │  (local machine)          │                      │
   │                           │                      │
   │─ Update Secrets ─────────▶│                      │
   │  (replace old keys)       │                      │
   │                           │                      │
   │─ Regenerate evidence      │                      │
   │  (sign with new key)      │                      │
   │                           │                      │
   │─ git push ───────────────▶│                      │
   │                           │                      │
   │                           │─ Trigger workflow ──▶│
   │                           │                      │
   │                           │                      │─ Use NEW keys
   │                           │                      │─ Sign evidence
   │                           │                      │─ Commit
   │                           │◀─────────────────────│
   │                           │                      │
   │◀─ New evidence ───────────│                      │
   │  (signed with new key)    │                      │
```

### **Workflow 3: Auditor Verification**

```
Auditor                      GitHub                Evidence
   │                           │                      │
   │─ Request public key ─────▶│                      │
   │                           │                      │
   │◀─ Send public key ────────│                      │
   │                           │                      │
   │─ Download evidence ───────┼─────────────────────▶│
   │                           │                      │
   │◀─ Evidence files ─────────┼──────────────────────│
   │  (.gxp/evidence/*.jws)    │                      │
   │                           │                      │
   │─ Verify signatures        │                      │
   │  (with public key)        │                      │
   │                           │                      │
   │  ✅ Valid                 │                      │
   │  Evidence authentic       │                      │
```

---

## Security Layers

```
┌───────────────────────────────────────────────────────┐
│ Layer 1: File Permissions (chmod 600)                │
│ ────────────────────────────────────────────────────  │
│ Prevents other users on same machine from reading     │
└───────────────────────────────────────────────────────┘
                      │
                      ▼
┌───────────────────────────────────────────────────────┐
│ Layer 2: .gitignore (.rosie-keys/)                   │
│ ────────────────────────────────────────────────────  │
│ Prevents accidental git commit                        │
└───────────────────────────────────────────────────────┘
                      │
                      ▼
┌───────────────────────────────────────────────────────┐
│ Layer 3: GitHub Secrets (encrypted at rest)          │
│ ────────────────────────────────────────────────────  │
│ AES-256 encryption, can't be retrieved once added     │
└───────────────────────────────────────────────────────┘
                      │
                      ▼
┌───────────────────────────────────────────────────────┐
│ Layer 4: Password Manager (optional backup)          │
│ ────────────────────────────────────────────────────  │
│ End-to-end encryption, requires master password       │
└───────────────────────────────────────────────────────┘
                      │
                      ▼
┌───────────────────────────────────────────────────────┐
│ Layer 5: Strong Algorithm (ES256 / P-256)            │
│ ────────────────────────────────────────────────────  │
│ NIST-approved, industry standard, ~128-bit security   │
└───────────────────────────────────────────────────────┘
```

---

## Quick Reference Commands

```bash
# Generate keys
openssl ecparam -name prime256v1 -genkey -noout -out .rosie-keys/private-key.pem
openssl ec -in .rosie-keys/private-key.pem -pubout -out .rosie-keys/public-key.pem

# Display keys for GitHub Secrets
./scripts/display-keys.sh

# Test signing locally
npm run test:ci
npm run generate-evidence

# Verify signatures
npm run verify-evidence

# Check file permissions
ls -la .rosie-keys/

# Verify git-ignored
git status | grep rosie-keys  # Should be empty

# Backup to password manager
cat .rosie-keys/private-key.pem | pbcopy  # macOS
cat .rosie-keys/private-key.pem | xclip   # Linux
```

---

**Last Updated:** 2026-02-05
